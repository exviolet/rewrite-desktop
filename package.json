{
  "name": "rewrite-desktop",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun tauri dev --features mcp-bridge --config src-tauri/tauri.dev.conf.json",
    "build": "bun tauri build",
    "build:bin": "bun tauri build --no-bundle",
    "update-web": "git submodule update --remote web"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
