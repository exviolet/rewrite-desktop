{
  "name": "rewritebox-desktop",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun tauri dev",
    "build": "bun tauri build",
    "update-web": "git submodule update --remote web"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
